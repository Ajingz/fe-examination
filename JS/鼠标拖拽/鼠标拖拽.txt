鼠标拖拽效果（百度登录）：

流程步骤
1、鼠标在标题栏按下，浮层元素标记为可以拖动
2、鼠标开始移动，检测浮层元素标记的拖动是否可以拖动，是，浮层跟随鼠标一起移动
3、鼠标松开，标记元素不可拖动

close btn使用a元素，是为了方便设置hover伪类

获取元素对象
自动居中 登录浮层
自动全屏 遮罩

鼠标事件1：鼠标在标题栏按下，计算鼠标相对拖拽元素的左上角的坐标，并且标记元素可拖动
	2：
